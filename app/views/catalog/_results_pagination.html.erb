<% if @response.num_pages > 1 %>
 <div class="row record-padding <%= 'top' if top_pagination %>">
  <div class="span9">
    <div class="pagination">
      <%= paginate @response, :outer_window => 2, :theme => 'blacklight' %>
    </div>
  </div>
 </div>
<% end %>
