<div id="item_actions-dropdown" class="dropdown pull-right  hidden-phone">
  <span class="hide-text"><%= t('frda.show.item_actions') %></span>
  <ul class="css-dropdown">
    <li class="btn btn-mini">
      <%=t('frda.show.item_actions')%>
      <span class="caret"></span>
      <ul>
        <!-- <li><%#= link_to t('frda.sidebar.actions.print'), '#' %></li> -->
        <li class="cite">
         <%= link_to t('frda.sidebar.actions.cite'), citation_catalog_path(:id => @document), {:id => 'citeLink', :class => 'lightboxLink'} %>
        </li>
        <%- # extracted from show_tools partial %>
        <% if @document.respond_to?( :to_email_text ) %>
          <li class="email">
          <%= link_to t('blacklight.tools.email'), email_catalog_path(:id => @document), {:id => 'emailLink', :name => 'email', :class => 'lightboxLink'} %>
          </li>
        <%- end -%>
      </ul>
    </li>
  </ul>
</div>
